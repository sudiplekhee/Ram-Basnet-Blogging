<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Purple Form</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 min-h-screen pt-24 flex items-center justify-center p-4">
  
  <!-- Fixed Navbar Include -->
  <div class="fixed top-0 left-0 w-full z-10">
    <%- include("../partials/navbar") %>
  </div>

  <!-- Form -->
  <form method="POST" action="/addblog" class="bg-white p-8 rounded-lg shadow-lg w-full max-w-md">
    <h2 class="text-2xl font-bold text-purple-700 mb-6 text-center">Create Post</h2>

    <label for="title" class="block text-purple-700 font-semibold mb-1">Title</label>
    <input type="text" id="title" name="title" placeholder="Enter title" class="w-full mb-4 px-3 py-2 border border-purple-300 rounded-md focus:outline-none focus:ring-2 focus:ring-purple-500" required />

    <label for="subtitle" class="block text-purple-700 font-semibold mb-1">Subtitle</label>
    <input type="text" id="subtitle" name="subtitle" placeholder="Enter subtitle" class="w-full mb-4 px-3 py-2 border border-purple-300 rounded-md focus:outline-none focus:ring-2 focus:ring-purple-500" required />

    <label for="description" class="block text-purple-700 font-semibold mb-1">Description</label>
    <textarea id="description" name="description" rows="4" placeholder="Write your description here..."
      class="w-full mb-6 px-3 py-2 border border-purple-300 rounded-md focus:outline-none focus:ring-2 focus:ring-purple-500 resize-none"required></textarea>

    <button type="submit" class="w-full bg-purple-600 text-white font-semibold py-2 rounded-md hover:bg-purple-700 transition">Submit</button>
  </form>
</body>
</html>
